$kind: http-request
name: jobs
url: "{{baseUrl}}/api/v1/jobs"
method: GET
scripts:
  - type: afterResponse
    code: "const json = pm.response.json();\r

      \r

      if (json.data && json.data.length > 0) {\r

      \  pm.environment.set(\"jobPostId\", json.data[0].id);\r

      }"
    language: text/javascript
order: 1041572496335347
